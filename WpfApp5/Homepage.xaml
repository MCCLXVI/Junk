<Page x:Class="WpfApp5.Homepage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp5"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Homepage">

    <Page.Resources>
        <!-- Style for TextBlock for consistent look -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Roboto"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
    </Page.Resources>

    <Grid Background="#3674B5">
        <!-- Main Content Area with a Border to match the navigation panel -->
        <Border Background="#3674B5"
                CornerRadius="20"
                Margin="20" Padding="20">

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Title Text -->
                <TextBlock Text="Homepage" FontSize="30" Margin="0,0,0,20"/>
                <!-- Additional Content (buttons, images, etc.) can be added here in the future -->
                <TextBlock Text="Welcome to Toka Fitness!" FontSize="16" Foreground="White"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
